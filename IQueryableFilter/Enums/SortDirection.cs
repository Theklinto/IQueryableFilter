namespace IQueryableFilter.Enums
{
    /// <summary>
    /// Defines what direction the query should be sorted when using <see cref="IQueryableFilter.Extract"/>
    /// </summary>
    public enum SortDirection
    {
        Ascending,
        Descending,
    }
}
